# Servidor Minecraft local

Coloque aqui o `paper.jar` correspondente à versão configurada e aceite a EULA manualmente no primeiro boot.

Este diretório é apenas um espaço reservado para o ambiente de teste. O `.gitignore` evita versionar o jar, mundos, logs e cache.
